# =============================================================================
# 🔧 ОКРУЖЕНИЕ DOCKER COMPOSE
# =============================================================================
# Выберите окружение для запуска контейнеров
# dev  - режим разработки (hot reload, pgadmin, открытые порты)
# prod - режим продакшена (без hot reload, закрытые порты, больше ресурсов)
COMPOSE_ENV=dev

# =============================================================================
# 🤖 НАСТРОЙКИ TELEGRAM БОТА
# =============================================================================
# Токен бота от @BotFather
BOT_TOKEN=123123:ABCabc

# Язык по умолчанию (ru, en, etc.)
DEFAULT_LANGUAGE=ru

# URL для webhook (если используется)
# Для polling режима можно оставить пустым или закомментировать
WEBHOOK_URL=https://webhook.server.com/webhook

# Порт для webhook сервера
APP_PORT=5000

# =============================================================================
# 🐍 PYTHON
# =============================================================================
# Путь для импорта модулей
PYTHONPATH=bot

# Режим отладки (True/False)
# Автоматически переопределяется в docker-compose.dev.yml и docker-compose.prod.yml
DEBUG=True

# =============================================================================
# 🐘 POSTGRESQL
# =============================================================================
# Хост базы данных (имя сервиса из docker-compose)
PG_HOST=database

# Порт PostgreSQL
PG_PORT=5432

# Имя пользователя БД
PG_USER=bot_user

# Пароль БД (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ В ПРОДЕ!)
PG_PASSWORD=CHANGE_THIS_PASSWORD

# Имя базы данных
PG_DATABASE=tg_bot

# =============================================================================
# 🔴 REDIS
# =============================================================================
# Хост Redis (имя сервиса из docker-compose)
REDIS_HOST=redis

# Порт Redis
REDIS_PORT=6379

# Пользователь Redis (обычно default)
REDIS_USER=default

# Пароль Redis (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ В ПРОДЕ!)
REDIS_PASSWORD=CHANGE_THIS_PASSWORD

# Номер базы данных Redis (0-15)
REDIS_DATABASE=0

# Время жизни кэша в днях
REDIS_CACHE_TTL=7

# =============================================================================
# 🔍 PGADMIN (только для dev окружения)
# =============================================================================
# Email для входа в PgAdmin
PGADMIN_DEFAULT_EMAIL=admin@pgadmin.com

# Пароль для входа в PgAdmin
PGADMIN_DEFAULT_PASSWORD=CHANGE_THIS_PASSWORD

# Доступ к PgAdmin: http://localhost:5050

# =============================================================================
# 📊 ЛОГИРОВАНИЕ ОШИБОК В TELEGRAM
# =============================================================================
# Включить/выключить отправку логов в Telegram (True/False)
LOGGING_ENABLED=False

# ID чата для отправки логов (можно получить через @userinfobot)
LOGGING_CHAT_ID=-100123123123

# ID топика/треда для ошибок (если используется топик в группе)
ERRORS_THREAD_ID=1

# =============================================================================
# 👑 АДМИНИСТРАТОРЫ БОТА
# =============================================================================
# Список ID администраторов бота (через запятую в квадратных скобках)
# Получить свой ID можно через @userinfobot
# Пример: ADMIN_IDS=[123456789,987654321]
ADMIN_IDS=[]

# =============================================================================
# 💡 ИНСТРУКЦИЯ ПО НАСТРОЙКЕ
# =============================================================================
# 
# 1. Скопируйте этот файл:
#    cp .env.example .env
# 
# 2. ОБЯЗАТЕЛЬНО измените:
#    - BOT_TOKEN (получите у @BotFather)
#    - PG_PASSWORD (используйте надежный пароль)
#    - REDIS_PASSWORD (используйте надежный пароль)
#    - PGADMIN_DEFAULT_PASSWORD (для доступа к PgAdmin)
# 
# 3. Для разработки:
#    make dev
# 
# 4. Для продакшена:
#    - Измените все пароли на надежные!
#    - Убедитесь, что WEBHOOK_URL настроен правильно
#    - Настройте логирование ошибок
#    make prod
# 
# 5. Не коммитьте .env в git!
#    Файл .env должен быть в .gitignore
# 
# =============================================================================
